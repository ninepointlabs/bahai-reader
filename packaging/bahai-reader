#!/bin/sh
set -eu
if command -v uwsm-app >/dev/null 2>&1 && [ -n "${HYPRLAND_INSTANCE_SIGNATURE:-}" ]; then
    exec uwsm-app -- /usr/bin/python3 /usr/share/bahai-reader/reader.py "$@"
fi
exec /usr/bin/python3 /usr/share/bahai-reader/reader.py "$@"
